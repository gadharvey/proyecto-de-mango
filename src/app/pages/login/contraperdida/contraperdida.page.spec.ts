import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ContraperdidaPage } from './contraperdida.page';

describe('ContraperdidaPage', () => {
  let component: ContraperdidaPage;
  let fixture: ComponentFixture<ContraperdidaPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ContraperdidaPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
